# Database Port (Exposed to host, default to 5433 to avoid conflicts with default PostgreSQL)
DB_PORT=5433

# PostgreSQL Database Credentials
DB_NAME=refract_ecommerce_demo
DB_USER=refract_admin
DB_PASSWORD=refract_secure_password_2026
